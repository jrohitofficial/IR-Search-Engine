# Task 1 — Vertical Search Engine

See the [project root README](../README.md) for full setup, environment variables,
architecture notes and API documentation. Quick start:

```powershell
cd backend
python run.py --crawl   # one-off crawl, populates MongoDB
python run.py            # web app + scheduler at http://localhost:5001
pytest -v                 # test suite
```
